AVAC_outdir: avac/_output
base_bathymetry: base_topo.asc
bathymetry: topo.asc
topo_type: 3

output_format: ascii

t0: 0.

qinit:
  - ""

world_png: topo.png
flood_dilation: 0  # px
flood_seed:
  - nan
  - nan

mode: src  # either src or bc

# src
fgout_fgno: 1
fgmax_fgno: 0
min_alt_avac: 0.  # for mode="src" only
lake_alt: 0.
# bc
bc_size: 100

cell_size: 2  # m

amr_ratios:
  x: [2, 4, 8]
  y: [2, 4, 8]
  t: [2, 4, 8]

max_refinement: -1

dry_tolerance: 1e-3